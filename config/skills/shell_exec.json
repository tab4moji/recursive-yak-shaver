{
  "id": "shell_exec",
  "name": "Shell Execution",
  "description": "Standard tools for file processing and system operations.",
  "patterns": [
    {
      "task": "Identify the largest Python file path",
      "recommended": "path=$(find . -type f -name \"*.py\" -exec du -b {} + | sort -rn | head -n 1 | cut -f2-)",
      "output_type": "Path"
    },
    {
      "task": "Identify the smallest file path",
      "recommended": "path=$(find . -type f -exec du -b {} + | sort -n | head -n 1 | cut -f2-)",
      "output_type": "Path"
    },
    {
      "task": "Execute pylint on the specified path",
      "recommended": "content=$(pylint \"$path\")",
      "input_type": "Path",
      "output_type": "Content"
    },
    {
      "task": "Access and display the contents of the specified path",
      "recommended": "content=$(cat \"$path\")",
      "input_type": "Path",
      "output_type": "Content"
    },
    {
      "task": "Retrieve the current date and time",
      "recommended": "content=$(date '+%Y-%m-%d %H:%M:%S')",
      "output_type": "DateTime",
      "binding": "content"
    },
    {
      "task": "Identify the current directory path",
      "recommended": "path=$(pwd)",
      "output_type": "Path",
      "binding": "path"
    }
  ]
}
