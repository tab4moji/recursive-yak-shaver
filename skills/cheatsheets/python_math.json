{
  "id": "python_math",
  "name": "Python Mathematics",
  "description": "Numerical and symbolic calculations.",
  "patterns": [
    {
      "task": "Find prime numbers in a range",
      "recommended": "python3 -c \"import math; min_v=${min}; max_v=${max}; print([i for i in range(min_v, max_v + 1) if i > 1 and all(i % d for d in range(2, int(math.sqrt(i)) + 1))])\""
    }
  ]
}
